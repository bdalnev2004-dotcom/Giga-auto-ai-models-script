# Lower bounds, not exact pins. The original pins could not resolve at all:
# aiogram 3.13 wanted pydantic<2.10 while instagrapi 2.18.16 wanted >=2.12.5,
# and the older builds have no wheels for Python 3.12+. Bounds mark "this is the
# oldest version known to work"; run `pip freeze > requirements.lock` once the
# farm is live if you want byte-identical rebuilds.
aiogram>=3.31.0
aiohttp-socks>=0.10.1   # aiogram proxy support: needed where api.telegram.org is blocked
anthropic>=1.1.0,<2.0.0
SQLAlchemy[asyncio]>=2.0.35
asyncpg>=0.30.0
aiosqlite>=0.22.1   # local dev only: run without a Postgres server
alembic>=1.13.2
apscheduler>=3.10.4
redis>=5.0.8
python-dotenv>=1.0.1
httpx>=0.27.2
instagrapi>=2.18.18
google-api-python-client>=2.144.0
google-auth>=2.34.0
cryptography>=43.0.1
